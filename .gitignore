# Secrets - NEVER commit the API key
.env
.env.*
!.env.example

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test / tooling caches
.pytest_cache/
.playwright-mcp/

# Runtime memory data (the vector store + personal facts are local, not source).
# The procedural skills ARE source, so keep only those under version control.
memory/*
!memory/procedural_memory/
